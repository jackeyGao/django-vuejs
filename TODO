√ give rooms a name 
    - and let people change them
- require handles (or auto-generate anon-y handles)
- persist handles, let people change them
- work on ui/ux a bit
    - can we get a slack-ish layout, with chat at the bottom?
    - list of people who are "here"?
- include old-school django POST view?
    might make a good flow for the article, start with old-style and then add WSS
- remove the room.channel_group abstraction -- it's probably better for "real" code, but it makes the example harder to read.
- likewise, look over the article and code and simplify where possible
- app.json / heroku button
